1. OS 내부에는 프로토콜 스택이 존재합니다. 프로토콜 스택은 애플리케이션에서 보낸 의뢰를 받아 데이터를 송/수신 하는데, 이때 사용하는 프로토콜이 무엇인가요?
    * 그럼 TCP와  UDP 프로토콜 각각이 사용되는 경우는 언제인가요?
2. 프로토콜 스택의 아래에는 IP 프로토콜을 사용하여 패킷 송/수신 동작을 제어하는 부분이 있습니다. 이 부분의 내부에는 ICMP와 ARP라는 프로토콜을 다루는 부분이 포함되어 있는데, 이 각각의 프로토콜의 역할에 대해 설명해주세요.
    * MAC 주소가 무엇입니까?
3. 실제로 네트워크를 통해 데이터를 전송하고 수신하는 동작을 실행하는 것은 LAN 드라이버인가요, LAN 어댑터인가요?
4. 소켓의 제어 정보를 기록하는 메모리 영역은 처음부터 존재하는 것입니까?
    * 소켓을 생성한다는 것은 어떤 의미입니까?
5. 모든 OS의 프로토콜 스택을 만드는 방법이 같다? 
6. MTU와 MSS의 차이점이 무엇인가요?
7. 초기 Sequence Number인 ISN을 0부터 시작하지 않고 난수를 생성해서 설정하는 이유가 무엇인가요?
8. 만약 Server에서 FIN 플래그를 전송하기 전에 전송한 패킷이 Routing 지연이나 패킷 유실로 인한 재전송 등으로 인해 FIN 패킷보다 늦게 도착하는 상황이 발생하면 어떻게 될까요?
9. 패킷을 건네줄 상대를 나타내는 수신처 IP 주소는 TCP 헤더와 IP 헤더 중 어느 것에 포함됩니까?
10. 포트 번호는 서버 프로그램의 종류를 지정하기 위해 주로 사용하는데, 이것은 TCP 헤더와 IP 헤더 중 어느 것에 포함됩니까?
11. 패킷이 올바르게 도착했는지 확인하는 것은 TCP와 IP 중 어느 것에 포함됩니까?
    * TCP의 연결 설정 과정(3단계)과 연결 종료 과정(4단계)이 단계가 차이나는 이유가 무엇인가요?
